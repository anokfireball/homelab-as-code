apiVersion: v2
name: gateway-route
description: Shared Certificate + ListenerSet + ReferenceGrant (+ optional HTTPRoute/redirect) for exposing a service via the shared Envoy Gateway.
type: application
version: 0.2.0
appVersion: "1.0"
