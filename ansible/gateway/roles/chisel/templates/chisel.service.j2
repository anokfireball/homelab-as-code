[Unit]
Description=Chisel Tunnel
Wants=network-online.target
After=network-online.target
StartLimitIntervalSec=0

[Install]
WantedBy=multi-user.target

[Service]
Restart=always
RestartSec=1
User=root
ExecStart=/usr/bin/chisel server --port={{ chisel_port }} --reverse --authfile /opt/chisel.auth
