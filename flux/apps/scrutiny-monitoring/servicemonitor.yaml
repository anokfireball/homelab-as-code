apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: scrutiny-web
  namespace: monitoring-system
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: scrutiny-web
  endpoints:
    - port: http
      path: /api/metrics
      interval: 60s
